//name triger: after_user_insert
//table: users
//time: AFTER
//Event: Insert

BEGIN
  IF (NEW.role = 'Admin')
  THEN
    INSERT INTO Admin (AdminID) VALUES (users.id);
  ELSE
    INSERT INTO Customer (CustomerID) VALUES (users.id);
  END IF;
END
